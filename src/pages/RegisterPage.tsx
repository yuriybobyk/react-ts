import React, {FC} from 'react';
import {RegisterForm} from "../components";

const RegisterPage: FC = () => {
    return (
        <div>
            <RegisterForm/>
        </div>
    );
};

export {RegisterPage};
